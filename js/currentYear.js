// Get the current year
      const currentYear = new Date().getFullYear();

      // Set the year dynamically in the element with id 'current-year'
      document.getElementById('current-year').textContent = currentYear;